{
  "name": "http-service-stub",
  "version": "1.0.0",
  "description": "Stub GET requests for HTTP endpoints",
  "author": "Anthony Green",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "pretest": "npm install",
    "test": "mocha test/*",
    "start": "node index.js",
    "stop": "killall node",
    "speculate": "speculate"
  },
  "engines": {
    "node": ">= 4.4.7"
  },
  "license": "MIT",
  "dependencies": {
    "newrelic": "1.27.1"
  },
  "devDependencies": {
    "mocha": "^2.5.3",
    "supertest": "^1.2.0",
    "speculate": "^1.5.0"
  },
  "spec": {
    "post": [
      "mv /usr/lib/thumbnail-discovery/bake-scripts /etc/bake-scripts/thumbnail-discovery",
      "mv /usr/lib/thumbnail-discovery/httpd-conf/http_vhost.conf /etc/httpd/conf.d/http_vhost.conf",
      "mv /usr/lib/thumbnail-discovery/httpd-conf/cloud_ssl_no_certs.conf /etc/httpd/conf.d/cloud_ssl_no_certs.conf"
    ],
    "executable": [
      "./bake-scripts"
    ],
    "requires": [
      "cloud-httpd24-ssl-includes"
    ]
  }
}
